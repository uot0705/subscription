<%= render "shared/header" %>
<h2 class="views-title">ユーザー編集</h2>
<%= form_with model: current_user, url: user_registration_path, method: :put, local: true do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

 <div class='form-wrap'>
  
  <div class = "form-box">
    <%= image_tag "user.png", class:"newicon-image" %>
    <div class="form-group">
      <div class='form-text-wrap'>
        <label class="form-text">ニックネーム</label>
      </div>
        <%= f.text_field :nickname,class:"input-default", id:"nickname", placeholder:" 例) Tarou",  maxlength:"20" %>
    </div>
  </div>

  <div class = "form-box">
    <%= image_tag "email.png", class:"newicon-image" %>
    <div class="form-group">
      <div class='form-text-wrap'>
       <label class="form-text">email</label>
      </div>
        <%= f.email_field :email, class:"input-default", id:"email", placeholder:" PC・携帯どちらでも可", autofocus: true %>
    </div>
  </div>
 
<div class = "form-box-c">
    <%= image_tag "check.png", class:"newicon-image" %>
    <div class="form-group">
      <div class='form-text-wrap'>
           <label class="form-text-c">毎月のサブスク契約を登録頂いたメールに配信します！</label>
       <p class="form-text-c">配信頻度をお選びください。</p>
      </div>
      <label><%= f.radio_button :check, "月1" %> 月1(毎月1日) </label>
       <label><%= f.radio_button :check, "月2" %> 月2(毎月1・15日) </label>
       <label><%= f.radio_button :check, "なし" %> なし </label>
    </div>
</div>

<div class="edit-actions">
    <%= f.submit "更新" , :class=> "submit-botan" %>
</div>

  <div class="display-flex-m-top">
  <p><%= link_to "退会", user_registration_path(current_user.id) ,method: :delete, class: "content-edit-dellet", "data-confirm" => "本当に退会しますか？"%></p>
  <p><%=link_to '戻る', root_path, class: "content-edit-dellet" %></p>
  </div>

</div>
<% end %>


